{
  "name": "hourglass-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "Playwright end-to-end tests for Hourglass (run against an isolated, file-backed instance so real cron jobs are never touched).",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
